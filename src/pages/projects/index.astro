---
// Importing necessary components
import MainLayout from "@/layouts/MainLayout.astro";
import PrimaryCTA from "@components/ui/buttons/PrimaryCTA.astro";
import CardSmall from "@components/ui/cards/CardSmall.astro";
import CardWide from "@components/ui/cards/CardWide.astro";
import FeaturesStatsAlt from "@components/sections/features/FeaturesStatsAlt.astro";
import TestimonialsSectionAlt from "@components/sections/testimonials/TestimonialsSectionAlt.astro";

// Importing necessary functions from Astro
import { getCollection } from "astro:content";
import type { CollectionEntry } from "astro:content";
import { SITE } from "@data/constants";

// Fetching all the product related content and sorting it by main.id
const product: CollectionEntry<"products">[] = (
  await getCollection("products", ({ id }) => {
    return id.startsWith("en/");
  })
).sort(
  (a: CollectionEntry<"products">, b: CollectionEntry<"products">) =>
    a.data.main.id - b.data.main.id
);

// Define variables for page content
const title: string = "客户案例";
const subTitle: string =
  "聚焦补全星科技在农业社会化服务、无人机核心产品、无人车平台与场景化交付中的真实落地实践。";

// Testimonial data that will be rendered in the component
const testimonials = [
  {
    content:
      "“补全星科技帮助我们把农事服务调度、设备管理和过程留痕真正串了起来，项目上线后跨乡镇协同明显顺畅，客户对服务透明度也更认可。”",
    author: "周海林",
    role: "区域农业服务中心负责人",
    avatarSrc:
      "https://images.unsplash.com/photo-1500648767791-00dcc994a43e?q=80&w=1374&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=facearea&facepad=2&w=320&h=320&q=80",
    avatarAlt: "农业服务中心客户头像",
  },
  {
    content:
      "“我们原本担心无人机与现场流程脱节，补全星科技在设备、平台和培训交付上做得很扎实，首批试点很快就形成了可复制的作业标准。”",
    author: "刘敏",
    role: "智慧农业项目经理",
    avatarSrc:
      "https://images.unsplash.com/photo-1544005313-94ddf0286df2?q=80&w=1376&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D8&auto=format&fit=facearea&facepad=2&w=320&h=320&q=80",
    avatarAlt: "智慧农业客户头像",
  },
  {
    content:
      "“从无人车试点到多场景扩展，补全星科技不仅交付了平台，还把后续运营方式一起梳理清楚，团队对长期推进更有信心。”",
    author: "陈致远",
    role: "园区智能化运营负责人",
    avatarSrc:
      "https://images.unsplash.com/photo-1474176857210-7287d38d27c6?q=80&w=1470&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D8&auto=format&fit=facearea&facepad=2&w=320&h=320&q=80",
    avatarAlt: "园区客户头像",
  },
];

const pageTitle: string = `客户案例 | ${SITE.title}`;
const metaDescription =
  "查看补全星科技在农业社会化服务、无人机、无人车与协同交付方向的客户案例与落地经验。";
const ogTitle = `客户案例 | ${SITE.title}`;
const projectsUrl = new URL("/projects", SITE.url).toString();
---

<MainLayout
  title={pageTitle}
  customDescription={metaDescription}
  customOgTitle={ogTitle}
  structuredData={{
    "@context": "https://schema.org",
    "@type": "WebPage",
    "@id": projectsUrl,
    url: projectsUrl,
    name: `客户案例 | ${SITE.title}`,
    description:
      "查看补全星科技在农业社会化服务、无人机核心产品、无人车平台与场景化交付方向的客户案例。",
    isPartOf: {
      "@type": "WebSite",
      url: SITE.url,
      name: SITE.title,
      description: SITE.description,
    },
    inLanguage: "zh-CN",
  }}
>
  <div
    class="mx-auto w-full max-w-(--breakpoint-2xl) flex-grow px-4 sm:px-6 lg:px-8"
  >
    <div
      class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
    >
      <div
        class="mb-4 flex items-center justify-between gap-8 sm:mb-8 md:mb-12"
      >
        <div class="flex items-center gap-12">
          <h1
            class="text-2xl font-bold tracking-tight text-balance text-neutral-800 md:text-4xl md:leading-tight dark:text-neutral-200"
          >
            {title}
          </h1>
          {
            subTitle && (
              <p class="hidden max-w-(--breakpoint-sm) text-pretty text-neutral-600 md:block dark:text-neutral-400">
                {subTitle}
              </p>
            )
          }
        </div>
        <PrimaryCTA title="查看客户反馈" url="#testimonials" noArrow={true} />
      </div>
      {
        /* Displaying products in alternating styles. Alternative product gets different card styling.
    Maps through all product entries and displays them with either CardSmall or CardWide based on their position. */
      }
      <section class="grid grid-cols-1 gap-4 sm:grid-cols-3 md:gap-6 xl:gap-8">
        {
          product.map((product, index) => {
            const position = index % 4;
            if (position === 0 || position === 3) {
              return <CardSmall product={product} />;
            } else {
              return <CardWide product={product} />;
            }
          })
        }
      </section>
    </div>
    {/* Features statistics section */}
    <FeaturesStatsAlt
      title="为什么选择补全星科技"
      subTitle="我们将农业社会化服务经验、智能装备研发能力与场景交付方法结合起来，帮助客户从试点验证走向规模落地，在真实业务环境中持续创造可衡量的成果。"
      benefits={[
        "聚焦农业与无人化场景，方案更贴近真实业务流程与现场约束。",
        "覆盖平台、设备、实施与培训协同，减少从方案到落地的断层。",
        "坚持长期陪跑与迭代优化，帮助客户沉淀可复制的运营能力。",
      ]}
    />
    {/* Testimonials section */}
    <TestimonialsSectionAlt
      title="客户反馈"
      testimonials={testimonials}
    />
  </div>
</MainLayout>
