import {
  FOOTER_NAVIGATION,
  PRIMARY_NAVIGATION,
} from "@data/company";

const navBarLinks = PRIMARY_NAVIGATION;

const footerLinks = FOOTER_NAVIGATION;

const socialLinks = {};

export default {
  navBarLinks,
  footerLinks,
  socialLinks,
};
