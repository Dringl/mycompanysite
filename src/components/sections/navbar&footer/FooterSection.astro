---
import navigation from "@utils/navigation.ts";
import { COMPANY_INFO, COMPANY_LINKS, aboutPageContent } from "@data/company";
import brandIcon from "@images/logo.png";
import { Image } from "astro:assets";

const strings = navigation;
const brandDescription = COMPANY_INFO.footerNote;
const contactIntro = COMPANY_INFO.contactIntro;
const currentYear = new Date().getFullYear();
---

<footer class="w-full border-t border-[color:var(--border-soft)] bg-[color:var(--surface-1)]/96 backdrop-blur-sm">
  <div
    class="mx-auto w-full max-w-[85rem] px-4 py-10 sm:px-6 lg:px-16 lg:pt-20 2xl:max-w-(--breakpoint-2xl)"
  >
    <div class="grid grid-cols-2 gap-6 md:grid-cols-4 lg:grid-cols-5">
      <div class="col-span-full lg:col-span-2">
        <Image src={brandIcon} alt={`${COMPANY_INFO.shortName} Logo`} class="mt-[-1rem] h-auto w-40" />
        <div class="mt-2 max-w-md space-y-3">
          <h3 class="text-lg font-bold text-[color:var(--text-strong)]">
            {COMPANY_INFO.name}
          </h3>
          <p class="text-sm leading-6 text-[color:var(--text-body)]">
            {brandDescription}
          </p>
        </div>
      </div>
      {
        strings.footerLinks.map((section) => (
          <div class="col-span-1">
            <h3 class="font-bold text-[color:var(--text-strong)]">
              {section.section}
            </h3>
            <ul class="mt-3 grid space-y-3">
              {section.links.map((link) => (
                <li>
                  <a
                    href={link.url}
                    class="inline-flex gap-x-2 rounded-lg text-[color:var(--text-body)] outline-hidden ring-zinc-500 transition duration-300 hover:text-primary-blue focus-visible:ring-3 dark:ring-zinc-200"
                  >
                    {link.name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }

      <div class="col-span-2">
        <h3 class="font-bold text-neutral-800 dark:text-neutral-200">
          联系我们
        </h3>
        <div class="mt-3 space-y-4">
          <p class="text-sm leading-6 text-[color:var(--text-body)]">
            {contactIntro}
          </p>
          <a
            href={COMPANY_LINKS.contact}
            class="inline-flex items-center justify-center rounded-full bg-primary-blue px-5 py-2 text-sm font-semibold text-white transition duration-300 hover:bg-primary-blue/80 focus-visible:outline-hidden focus-visible:ring-3 focus-visible:ring-zinc-500 dark:focus-visible:ring-zinc-200"
          >
            {COMPANY_INFO.ctaLabel}
          </a>
        </div>
      </div>
    </div>

    <div
      class="mt-9 grid gap-y-2 border-t border-[color:var(--border-soft)] pt-6 sm:mt-12 sm:flex sm:items-center sm:justify-between sm:gap-y-0"
    >
      <div class="flex items-center justify-between">
        <p class="text-sm text-[color:var(--text-muted)]">
          © {currentYear} {COMPANY_INFO.name}。{aboutPageContent.footer.copyright}
        </p>
      </div>

      <div>
        <a
          href={COMPANY_LINKS.contact}
          class="rounded-lg text-sm font-medium text-[color:var(--text-body)] underline underline-offset-2 outline-hidden ring-zinc-500 transition duration-300 hover:text-primary-blue hover:decoration-dashed focus-visible:ring-3 dark:ring-zinc-200"
        >
          {aboutPageContent.footer.ctaTitle}
        </a>
      </div>
    </div>

  </div>
</footer>
